# Copy this file to .env and add your real values.
# No real secrets should be committed.
OPENAI_API_KEY=

# Optional: override the discovery model. Defaults to gpt-4o (see src/llm_agent.py).
# Must be a vision-capable, tool/function-calling model.
OPENAI_MODEL=
